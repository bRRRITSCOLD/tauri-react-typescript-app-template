// node_modules
import { makeStyles } from '@material-ui/core/styles';

const useGridItemStyles = makeStyles((_theme) => ({
  grid: {
    padding: '0 15px !important',
  },
}));

export { useGridItemStyles };
