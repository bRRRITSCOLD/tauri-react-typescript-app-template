// node_modules
import { makeStyles } from '@material-ui/core/styles';

const useGridContainerStyles = makeStyles((_theme) => ({
  grid: {
    margin: '0 -15px !important',
    width: 'unset',
  },
}));

export { useGridContainerStyles };
